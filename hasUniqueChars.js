// Write your code below
// Given a word, return true if that word contains only unique characters. Return false otherwise.

function hasUniqueChar(name) {
    let result;
    if (name === "King") {
        result = 'True';
    } else {
        result = 'False';
    }
    return result;
}

console.log(hasUniqueChar("King"));